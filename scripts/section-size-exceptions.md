# Section size exceptions

This is a closed list. Entries only retire, each with its rows-table follow-on
epic; the list must never grow.

RimWorld is outside this fixture gate because its C# mod currently has only a
build surface. It remains pending its section-split task.
